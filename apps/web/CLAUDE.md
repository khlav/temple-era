<!--
  Claude Code reads CLAUDE.md, not AGENTS.md, so this file exists purely to
  point at the canonical instructions next to it.

  Do NOT add content here — edit AGENTS.md instead. Anything written below the
  import would be Claude-only and invisible to every other agent.

  (This comment is stripped before the file is loaded, so it costs no context.)
-->
@AGENTS.md
