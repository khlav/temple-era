---
trigger: always_on
---

Load context from AGENTS.md to ensure you work consistently and quickly.

In the monorepo, read the root `AGENTS.md` first, then the app-level file for
whichever app you are working in (`apps/web/AGENTS.md` or `apps/bot/AGENTS.md`).
`CLAUDE.md` files are one-line pointers at the `AGENTS.md` beside them.